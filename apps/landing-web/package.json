{
  "name": "@taptime/landing-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:browser": "node --test tests/browser.test.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vite": "8.1.3",
    "vitest": "^4.1.9",
    "jsdom": "^26.1.0",
    "axe-core": "^4.10.3",
    "playwright-core": "1.62.1"
  }
}
