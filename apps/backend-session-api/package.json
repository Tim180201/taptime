{
  "name": "@taptime/backend-session-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private C1 Node 24 transport exposing only server-authoritative GET /v1/session.",
  "engines": {
    "node": ">=24 <25"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json --emitDeclarationOnly && esbuild src/index.ts src/main.ts --bundle --platform=node --target=node24 --format=esm --external:@taptime/backend-identity --external:@taptime/core --external:pg --sourcemap --outdir=dist",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@taptime/backend-identity": "*",
    "@taptime/core": "*",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@taptime/backend-schema": "*",
    "@types/node": "^24.10.13",
    "@types/pg": "^8.20.0",
    "esbuild": "^0.28.1",
    "jose": "^6.2.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
