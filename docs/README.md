# docs/

This folder is for general documentation that sits outside the FDOS-governed knowledge base.

## docs/ vs. ADO/ — what goes where

`ADO/` is the governed knowledge base: Product Vision, Product Principles, ADRs, the Feature Blueprint Standard, the Decision Log, the Risk Register and Evidence. Everything in `ADO/` follows the FDOS approval process (Human Architect sign-off, versioning, traceability, Quality Gates) — see `CONTRIBUTING.md` and `ADO/README.md`.

`docs/` is for material that explains rather than decides, and does not require that governance overhead:

- onboarding / getting-started guides for new contributors
- generated API documentation (once code exists)
- diagrams, walkthroughs, and explanatory material referencing approved ADO content

If a document records a decision, defines a standard, or needs an approval trail, it belongs in `ADO/`, not here — even if it feels like "just documentation."

Nothing here yet; this file exists so the distinction is explicit before the folder fills up.
