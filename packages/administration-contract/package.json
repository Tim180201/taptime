{
  "name": "@taptime/administration-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Administration names, command digests, and versioned setup-projection contracts shared by server and clients.",
  "engines": {
    "node": ">=24 <25"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./setup-projection": {
      "types": "./dist/setupProjection.d.ts",
      "import": "./dist/setupProjection.js"
    },
    "./managed-people": {
      "types": "./dist/managedPeople.d.ts",
      "import": "./dist/managedPeople.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json --emitDeclarationOnly && esbuild src/index.ts src/setupProjection.ts src/managedPeople.ts --bundle --platform=node --target=node24 --format=esm --external:@taptime/core --sourcemap --outdir=dist",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@unicode/unicode-15.1.0": "1.6.17",
    "@taptime/core": "*"
  },
  "devDependencies": {
    "@types/node": "^24.10.13",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
