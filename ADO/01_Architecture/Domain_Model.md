# Domain Model

Status: Sprint 1 Draft  
Owner: Technical Lead  
Date: 2026-06-26

## Product Domain

TapTim.e is a professional time tracking product for small and medium-sized businesses.

Its initial differentiator is NFC-based confirmation of work events through mobile devices.

The product must not be modeled as "NFC time tracking only". NFC is a primary trigger mechanism, but the business domain is event-based workforce and time tracking.

## Core Domain Idea

```text
Trigger
  -> Work Event
  -> Business Rules
  -> Time Entry
```

A trigger can be an NFC scan in version 1. Later it may also be a QR code, manual action, API call or other input mechanism.

## Known Domain Concepts

| Concept | Description | Status |
|---|---|---|
| Organization | Business/customer context using TapTim.e. | Draft |
| User | Person with access to the system. | Draft |
| Employee | User or person whose work time is tracked. | Draft |
| Role | Permission category assigned to a user. | Draft |
| Customer | External or internal target for work, e.g. student, client, job site context. | Draft |
| Project/Job | Trackable work context. | Draft |
| Location | Physical place relevant for work tracking. | Draft |
| NFC Tag | Physical chip with a technical identifier. | Draft |
| NFC Assignment | Business assignment that maps an NFC tag to a target. | Accepted by ADR-0002 |
| Assignment Target | Entity an NFC tag can resolve to, initially customer, later project/location/machine/etc. | Draft |
| Scan Event | Raw event generated by scanning an NFC tag. | Draft |
| Work Event | Normalized business event derived from a trigger. | Draft |
| Time Entry | Validated business record representing tracked time. | Draft |
| Export | Generated reporting artifact. | Open |
| Approval | Review and confirmation of time entries. | Open |

## Important Separation

Raw NFC scan events, work events and time entries are separate concepts.

```text
NFC Scan Event
  -> Work Event
  -> Time Entry
```

A scan is technical evidence of an interaction.

A work event is a normalized business signal.

A time entry is the validated business record used for reporting, approval and export.

## NFC Assignment Model

TapTim.e version 1 may present the simple workflow:

```text
Assign NFC tag to customer
```

Internally, the model remains:

```text
NFC Tag
  -> NFC Assignment
  -> Assignment Target
```

This allows the first use case to remain simple while keeping future target types possible.

## Initial Target Types

| Target Type | Version | Example |
|---|---|---|
| Customer | v1 | Student, client, service customer |
| Project/Job | Later | Construction project, maintenance job |
| Location | Later | Office, room, site, station |
| Machine | Later | Machine-specific work tracking |
| Vehicle | Later | Vehicle-based mobile workforce tracking |
| Workflow | Later | Start inventory, start service process |

## Role Model Draft

| Role | Purpose | Initial Permissions |
|---|---|---|
| System Owner | Owns the organization account. | Full control, billing/governance later |
| Administrator | Manages the organization. | Users, customers, NFC tags, settings |
| Team Lead | Supervises operational work. | Review time entries, view team reports |
| Employee | Performs work. | Scan NFC tags, start/stop work, view own time |

## First Business Scenario

```text
Teacher visits student
  -> Teacher scans student's NFC card
  -> System resolves NFC tag to customer/student
  -> System creates work event
  -> Business rules start lesson time
  -> Teacher scans same card again
  -> System creates second work event
  -> Business rules stop lesson time
  -> Validated time entry exists
```

## Product Value in This Scenario

- The teacher does not need to search manually for the student.
- The student actively participates in confirming the lesson.
- The NFC card creates a sense of belonging to the company.
- The time entry is based on a stronger interaction than manual input alone.

## Open Modeling Questions

- Can one scan both start and stop time depending on current state? — **Proposed answer in ADR-0008** (Recommendation, not yet reviewed by Technical Lead): toggle-based, scan starts if no open session exists for the target, scan stops if one does. Note: FB-001/TS-001 on `main` already establish the business-rule level ("duplicate scans shall not create duplicate TimeEntries"); ADR-0008 only proposes the mechanism-level detail — see `ADO/05_Evidence/EP-002/Repository_Reconciliation.md`.
- How should duplicate scans be handled? — see above; mechanism-level detail proposed in ADR-0008, business rule already covered by FB-001.
- What happens if the mobile device is offline? — covered by ADR-0004; local event queue and sync design due in Sprint 6.
- Is customer confirmation legally or operationally relevant, or only practical evidence? — still open, no proposal yet.
- Can one NFC tag have multiple assignments over time? — proposed in ADR-0008 (Recommendation): yes, via new Assignment records over time.
- Should assignment history be immutable for auditability? — proposed in ADR-0008 (Recommendation): yes, Assignments are never overwritten, only superseded.
- Which roles may assign or reassign NFC tags? — proposed in ADR-0008 (Recommendation): System Owner and Administrator only, per the existing Role Model permission matrix.

## Proposed Addition (Recommendation, not yet reviewed)

`ADR-0003` Revision 2026-07-02 proposes an **Open Session Reminder** concept for v1 scope: a time-based, non-GPS local reminder triggered when a Work Event has been open unusually long without a stop scan. Pending Technical Lead review — see `ADO/05_Evidence/EP-002/Research_Agent_Recommendations.md`.
