{
  "name": "taptime",
  "private": true,
  "version": "0.1.0",
  "description": "TapTim.e monorepo root. See ADO/README.md for the engineering knowledge base.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
