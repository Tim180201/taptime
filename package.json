{
  "name": "taptime",
  "private": true,
  "version": "0.1.0",
  "description": "TapTim.e monorepo root. See ADO/README.md for the engineering knowledge base.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=@taptime/core && npm run test --workspace=@taptime/mobile && npm run test --workspace=@taptime/admin-web",
    "build": "npm run build --workspaces --if-present",
    "build:contracts": "npm run build --workspace=@taptime/administration-contract && npm run build --workspace=@taptime/mobile-work-contract && npm run build --workspace=@taptime/offline-sync-contract && npm run build --workspace=@taptime/time-entry-export-contract && npm run build --workspace=@taptime/time-review-contract",
    "typecheck": "npm run typecheck --workspace=@taptime/core && npm run typecheck --workspace=@taptime/mobile && npm run typecheck --workspace=@taptime/admin-web"
  }
}
